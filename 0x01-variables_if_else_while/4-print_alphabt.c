#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Writtinh alphabet
 *
 * Return: 0
 */
int main(void)
{
	char ls;

	for (ls = 'a'; ls <= 'z'; ls++)
	{
		if (lc != 'q' && lc != 'e')
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
